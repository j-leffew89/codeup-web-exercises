"use strict";

//VARIABLES
// A variable is a symbolic representative of a value that can be different in each run of a program
// The variable is a pointer to a stored place in memory of your runny application

var name = "Jesse";
var expectedName = "Jesse";

if (name === expectedName){
    console.log("You got the right name!");
}